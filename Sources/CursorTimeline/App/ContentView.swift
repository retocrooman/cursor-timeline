import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Cursor Timeline")
            .font(.title2)
            .frame(minWidth: 480, minHeight: 320)
    }
}

#Preview {
    ContentView()
}
